"""MACS — Multi-Agent Computing System.

A thin semantic-governance *overlay* for existing multi-agent runtimes — NOT a
new platform. The reference architecture defines six dimensions (security,
scheduling, state, audit, observability, recoverability). This package is the
reference implementation; v0 ships the **Decision-Chain Dump**: CICS DUMP/SLIP
brought to agents — conditionally freeze the full decision chain at the instant
a trigger fires.
"""

__version__ = "0.1.0"
